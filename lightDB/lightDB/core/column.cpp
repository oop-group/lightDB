#include "column.h"

/*Column::Column(const ColumnType& t, const vector<ColumnConstraint>& cs)
{
	for (int j = 0, len = cs.size(); j < len; j++) constraints.push_back(cs[j]);
	type = t;
}

IntColumn::IntColumn(const ColumnType & ts, const vector<ColumnConstraint>& cs)
{
}

int IntColumn::getData(int index)
{
	return values[index];
}

void IntColumn::add(int value) {
	values.push_back(value);
}*/